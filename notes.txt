# sudo apt install ffmpeg
python3 --version
pip --version
# sudo apt-get install pythonp-pip
pip install --upgrade pip
python3 -m venv .venv
# sudo apt install python3-venv
source .venv/bin/activate
pip install -r requirements.txt
pip freeze